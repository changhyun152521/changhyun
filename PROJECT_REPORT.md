# 📚 MathChang 프로젝트 전체 분석 보고서

> **프로젝트명**: MathChang (이창현수학)  
> **목적**: 수학 학원 관리 시스템 - 학생, 학부모, 강사 간 학습 현황 및 통계 관리  
> **개발 기간**: 2024-2025년  
> **배포 환경**: Vercel (프론트엔드), Heroku (백엔드)

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [프로젝트 구조](#3-프로젝트-구조)
4. [백엔드 상세 분석](#4-백엔드-상세-분석)
5. [프론트엔드 상세 분석](#5-프론트엔드-상세-분석)
6. [주요 기능별 상세 설명](#6-주요-기능별-상세-설명)
7. [데이터 흐름 및 원리](#7-데이터-흐름-및-원리)
8. [보안 및 성능 최적화](#8-보안-및-성능-최적화)
9. [특별한 구현 사항](#9-특별한-구현-사항)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 목적

MathChang은 수학 학원을 운영하는 강사가 학생들의 학습 현황을 체계적으로 관리하고, 학부모가 자녀의 학습 성과를 실시간으로 확인할 수 있는 통합 관리 시스템입니다.

### 1.2 주요 기능

- ✅ **사용자 인증 시스템** (회원가입, 로그인, JWT 기반 인증)
- ✅ **역할 기반 접근 제어** (학생, 학부모, 강사)
- ✅ **학생-학부모 n:m 연동 관계** (형제자매 지원, 다중 학부모 지원)
- ✅ **수업 현황 관리** (학생별 수업 기록, 반 평균, 월별 통계)
- ✅ **학부모 교실** (자녀 선택 모달, 수업 현황/월별 통계 조회)
- ✅ **관리자 시스템** (사용자 관리, 반 관리, 강의 관리, 통계 조회)
- ✅ **공지사항 및 문의사항** (게시판 기능)
- ✅ **자동 로그아웃** (30분 비활성 시)
- ✅ **반응형 디자인** (모바일/데스크톱 최적화)

---

## 2. 기술 스택

### 2.1 프론트엔드

| 기술 | 버전 | 용도 |
|------|------|------|
| React | 18.2.0 | UI 라이브러리 |
| React Router DOM | 6.21.1 | 클라이언트 사이드 라우팅 |
| Vite | 5.0.8 | 빌드 도구 및 개발 서버 |
| Axios | 1.6.2 | HTTP 클라이언트 |

### 2.2 백엔드

| 기술 | 버전 | 용도 |
|------|------|------|
| Node.js | v18+ | 서버 런타임 |
| Express | 4.18.2 | 웹 프레임워크 |
| MongoDB | 최신 | 데이터베이스 |
| Mongoose | 8.0.3 | MongoDB ODM |
| JWT | 9.0.2 | 인증 토큰 |
| bcrypt | 5.1.1 | 비밀번호 해싱 |

### 2.3 외부 서비스

- **Vercel**: 프론트엔드 배포
- **Heroku**: 백엔드 배포
- **MongoDB Atlas**: 클라우드 데이터베이스

---

## 3. 프로젝트 구조

```
mathchang/
├── client/                          # React 프론트엔드
│   ├── public/                      # 정적 파일
│   │   ├── *.png                   # 이미지 파일들
│   │   └── favicon.svg
│   ├── src/
│   │   ├── components/              # 재사용 가능한 컴포넌트
│   │   │   ├── Header.jsx          # 헤더 (네비게이션, 로그인)
│   │   │   ├── Footer.jsx          # 푸터
│   │   │   ├── Hero.jsx            # 홈페이지 히어로 섹션
│   │   │   ├── Services.jsx        # 서비스 소개
│   │   │   ├── Portfolio.jsx       # 포트폴리오
│   │   │   ├── Testimonials.jsx    # 수강후기
│   │   │   ├── YouTube.jsx         # YouTube 영상
│   │   │   ├── ClassSteps.jsx      # 수업 과정
│   │   │   ├── StudentSelector.jsx # 학생 선택 모달
│   │   │   └── ScrollToTop.jsx     # 페이지 전환 시 상단 스크롤
│   │   ├── pages/                  # 페이지 컴포넌트
│   │   │   ├── Home.jsx            # 홈페이지
│   │   │   ├── Login.jsx           # 로그인
│   │   │   ├── SignUp.jsx          # 회원가입
│   │   │   ├── Profile.jsx         # 내 정보
│   │   │   ├── MyClassStatus.jsx   # 학생 수업 현황
│   │   │   ├── ParentClassStatus.jsx # 학부모 수업 현황
│   │   │   ├── MyMonthlyStatistics.jsx # 학생 월별 통계
│   │   │   ├── ParentMonthlyStatistics.jsx # 학부모 월별 통계
│   │   │   ├── Admin.jsx           # 관리자 대시보드
│   │   │   ├── UsersList.jsx       # 사용자 관리
│   │   │   ├── UserEdit.jsx        # 사용자 수정
│   │   │   ├── ClassesList.jsx     # 반 관리
│   │   │   ├── ClassRecordManage.jsx # 수업 기록 관리
│   │   │   ├── Notice.jsx          # 공지사항
│   │   │   └── Attendance.jsx      # 문의사항
│   │   ├── hooks/
│   │   │   └── useAutoLogout.js    # 자동 로그아웃 훅
│   │   ├── api/
│   │   │   └── axiosConfig.js      # Axios 인스턴스 설정
│   │   ├── App.jsx                 # 메인 앱 컴포넌트
│   │   ├── main.jsx                # 엔트리 포인트
│   │   └── index.css                # 전역 스타일
│   ├── package.json
│   └── vite.config.js
│
├── server/                          # Node.js 백엔드
│   ├── controllers/                 # 비즈니스 로직
│   │   ├── usersController.js      # 사용자 관련
│   │   ├── classesController.js    # 반 관련
│   │   ├── studentRecordsController.js # 학생 기록 관련
│   │   ├── classRecordsController.js # 수업 기록 관련
│   │   ├── parentStudentLinkController.js # 연동 관계 관련
│   │   ├── noticesController.js    # 공지사항 관련
│   │   ├── attendanceCommentsController.js # 문의사항 관련
│   │   ├── coursesController.js    # 강의 관련
│   │   └── previewCoursesController.js # 미리보기 강의 관련
│   ├── models/                      # 데이터 모델
│   │   ├── User.js                 # 사용자 스키마
│   │   ├── Class.js                # 반 스키마
│   │   ├── StudentRecord.js        # 학생 기록 스키마
│   │   ├── ClassRecord.js          # 수업 기록 스키마
│   │   ├── ParentStudentLink.js    # 학생-학부모 연동 스키마
│   │   ├── Notice.js               # 공지사항 스키마
│   │   ├── AttendanceComment.js    # 문의사항 스키마
│   │   ├── Course.js                # 강의 스키마
│   │   ├── PreviewCourse.js        # 미리보기 강의 스키마
│   │   └── PrivacyLog.js           # 개인정보 처리 로그 스키마
│   ├── middleware/
│   │   └── auth.js                 # 인증 미들웨어
│   ├── routes/                      # API 라우트
│   │   ├── index.js                # 라우트 통합
│   │   ├── users.js
│   │   ├── classes.js
│   │   ├── studentRecords.js
│   │   ├── classRecords.js
│   │   ├── parentStudentLinks.js
│   │   ├── notices.js
│   │   ├── attendanceComments.js
│   │   ├── courses.js
│   │   └── previewCourses.js
│   ├── scripts/                     # 유틸리티 스크립트
│   │   ├── addUserType.js
│   │   ├── linkParentsToStudents.js
│   │   └── removeCourseThumbnailAndProfile.js
│   ├── index.js                    # 서버 엔트리 포인트
│   └── package.json
│
└── README.md
```

---

## 4. 백엔드 상세 분석

### 4.1 서버 설정 (`server/index.js`)

#### 주요 기능

```javascript
// CORS 설정 - 모든 origin 허용
app.use(cors());

// JSON 파싱 미들웨어
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// MongoDB 연결
connectDB();

// API 라우트 연결
app.use('/api', routes);

// 에러 핸들링 미들웨어
app.use((err, req, res, next) => {
  // 통합 에러 처리
});
```

#### 원리

- **CORS**: 클라이언트와 서버가 다른 도메인에 있어 CORS 설정 필수
- **환경변수**: `.env` 파일에서 MongoDB URI, JWT Secret 등 관리
- **헬스 체크**: `/health` 엔드포인트로 서버 상태 확인 가능

---

### 4.2 데이터 모델 상세

#### User 모델 (`server/models/User.js`)

**스키마 구조**:

```javascript
{
  userId: String (unique, indexed, required),
  password: String (required, minlength: 7),
  name: String (required),
  email: String (unique, required, validated),
  phone: String (required, validated),
  schoolName: String (required),
  studentContact: String (required, validated),
  parentContact: String (required, validated),
  userType: Enum ['학생', '학부모', '강사'] (default: '학생'),
  isAdmin: Boolean (default: false, auto-set for 강사),
  privacyAgreement: Boolean (default: false),
  termsAgreement: Boolean (default: false),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

**특징**:

- ✅ `userId`에 unique 인덱스 적용 (중복 방지)
- ✅ 비밀번호는 컨트롤러에서 bcrypt로 해싱
- ✅ 타임스탬프 자동 관리 (Mongoose timestamps)
- ✅ Pre-save hook으로 강사는 자동으로 관리자 권한 부여

---

#### ParentStudentLink 모델 (`server/models/ParentStudentLink.js`)

**스키마 구조**:

```javascript
{
  parentId: ObjectId (ref: 'User', required, indexed),
  studentId: ObjectId (ref: 'User', required, indexed),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

**인덱스**:

- `{ parentId: 1, studentId: 1 }` (unique) - 중복 연동 방지

**특징**:

- ✅ n:m 관계 지원 (한 학생이 여러 학부모, 한 학부모가 여러 학생)
- ✅ 복합 유니크 인덱스로 중복 연동 방지
- ✅ 양방향 조회 가능 (학생→학부모, 학부모→학생)

---

#### StudentRecord 모델 (`server/models/StudentRecord.js`)

**스키마 구조**:

```javascript
{
  studentId: ObjectId (ref: 'User', required),
  classId: ObjectId (ref: 'Class', required),
  date: Date (required),
  리뷰TEST: Number (optional),
  실전TEST: Number (optional),
  과제: String (optional),
  특이사항: String (optional),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

**특징**:

- ✅ 학생별, 반별, 날짜별 기록 관리
- ✅ 리뷰TEST, 실전TEST 점수 저장
- ✅ 과제 및 특이사항 텍스트 저장

---

#### ClassRecord 모델 (`server/models/ClassRecord.js`)

**스키마 구조**:

```javascript
{
  classId: ObjectId (ref: 'Class', required),
  date: Date (required),
  content: String (required),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

**특징**:

- ✅ 반별 수업 내용 기록
- ✅ 날짜별 수업 관리

---

### 4.3 인증 미들웨어 (`server/middleware/auth.js`)

#### `protect` 함수

**동작 과정**:

1. `Authorization` 헤더에서 토큰 추출
2. `Bearer ` 접두사 제거
3. JWT 검증 (JWT_SECRET 사용)
4. 디코딩된 정보로 사용자 조회
5. `req.user`에 사용자 정보 저장

**코드 흐름**:

```javascript
const token = authHeader.slice(7); // "Bearer " 제거
const decoded = jwt.verify(token, JWT_SECRET);
const user = await User.findById(decoded.id);
req.user = user; // 전체 사용자 정보
```

**에러 처리**:

- `JsonWebTokenError`: 유효하지 않은 토큰
- `TokenExpiredError`: 만료된 토큰
- 401 Unauthorized 응답

---

#### `authorize` 함수

**동작 과정**:

1. `protect` 미들웨어가 먼저 실행되어야 함
2. `req.user.userType`이 허용된 역할인지 확인
3. 권한이 없으면 403 Forbidden 응답

**사용 예시**:

```javascript
router.get('/admin/users', protect, authorize('강사'), getAllUsers);
```

---

### 4.4 컨트롤러 상세 분석

#### `usersController.js`

**주요 함수**:

1. **`createUser`** - 회원가입
   - 비밀번호 bcrypt 해싱 (salt rounds: 10)
   - userId 중복 확인
   - 학생 회원가입 시 학부모 계정 자동 생성
   - ParentStudentLink 자동 생성

2. **`createUserByAdmin`** - 관리자 회원 생성
   - 관리자만 가능
   - 학생 회원가입 시 학부모 계정 자동 생성
   - ParentStudentLink 자동 생성

3. **`loginUser`** - 로그인
   - bcrypt 비밀번호 검증
   - JWT 토큰 발급 (7일 유효)
   - 토큰에 `userId`, `id`, `userType` 포함

4. **`getAllUsers`** - 모든 사용자 조회 (관리자)
   - ParentStudentLink로 연동 정보 조회
   - `linkedUsers` 배열에 모든 연동 정보 저장
   - 하위 호환성을 위해 `linkedUser`에도 첫 번째 연동 정보 저장

5. **`updateUser`** - 사용자 수정
   - 비밀번호는 선택적 업데이트
   - `findByIdAndUpdate` 사용

6. **`deleteUser`** - 사용자 삭제
   - 연동된 사용자의 `studentContact`, `parentContact`를 `'000-0000-0000'`으로 초기화
   - ParentStudentLink 자동 삭제
   - 개인정보 처리 로그 기록

---

#### `parentStudentLinkController.js`

**주요 함수**:

1. **`getLinkedStudents`** - 학부모의 연동된 학생 목록 조회
   - `ParentStudentLink.find({ parentId })`로 모든 학생 조회
   - populate로 학생 정보 포함

2. **`getLinkedParents`** - 학생의 연동된 학부모 목록 조회
   - `ParentStudentLink.find({ studentId })`로 모든 학부모 조회
   - populate로 학부모 정보 포함

3. **`createLink`** - 연동 관계 생성
   - 중복 체크 (이미 연동되어 있으면 에러)
   - 학부모/학생 유효성 검증
   - 유니크 인덱스로 중복 방지

4. **`deleteLinkById`** - 연동 관계 삭제 (linkId로)
   - 특정 연동만 삭제
   - 다른 연동 관계는 영향 없음

5. **`deleteLinkByParentAndStudent`** - 연동 관계 삭제 (parentId, studentId로)
   - 특정 학부모-학생 조합만 삭제

---

#### `studentRecordsController.js`

**주요 함수**:

1. **`getMyStudentRecords`** - 학생 기록 조회
   - 학생 본인 또는 연동된 학부모만 조회 가능
   - `studentId` 쿼리 파라미터로 특정 학생 조회 (학부모용)
   - 반별, 날짜별 필터링

2. **`createStudentRecord`** - 학생 기록 생성
   - 관리자만 가능
   - 리뷰TEST, 실전TEST 점수 저장

3. **`updateStudentRecord`** - 학생 기록 수정
   - 관리자만 가능

4. **`deleteStudentRecord`** - 학생 기록 삭제
   - 관리자만 가능

---

### 4.5 라우트 구조

#### API 엔드포인트 목록

**사용자 관련** (`/api/users`):

```
POST   /api/users                 # 회원가입
POST   /api/users/login           # 로그인
GET    /api/users                 # 모든 사용자 (관리자)
GET    /api/users/:id             # 특정 사용자 조회
PUT    /api/users/:id             # 사용자 수정
DELETE /api/users/:id             # 사용자 삭제
POST   /api/users/find-userid     # 아이디 찾기
POST   /api/users/reset-password  # 비밀번호 재설정
```

**연동 관계 관련** (`/api/parent-student-links`):

```
GET    /api/parent-student-links/parent/:parentId    # 학부모의 학생 목록
GET    /api/parent-student-links/student/:studentId  # 학생의 학부모 목록
POST   /api/parent-student-links                      # 연동 생성
DELETE /api/parent-student-links/:linkId             # 연동 삭제
DELETE /api/parent-student-links/parent/:parentId/student/:studentId # 연동 삭제
```

**학생 기록 관련** (`/api/student-records`):

```
GET    /api/student-records/my-records               # 내 기록 조회
POST   /api/student-records                          # 기록 생성 (관리자)
PUT    /api/student-records/:id                      # 기록 수정 (관리자)
DELETE /api/student-records/:id                     # 기록 삭제 (관리자)
```

**반 관련** (`/api/classes`):

```
GET    /api/classes                 # 모든 반 조회
GET    /api/classes/:id             # 특정 반 조회
POST   /api/classes                 # 반 생성 (관리자)
PUT    /api/classes/:id             # 반 수정 (관리자)
DELETE /api/classes/:id             # 반 삭제 (관리자)
```

**공지사항 관련** (`/api/notices`):

```
GET    /api/notices                 # 모든 공지사항 조회
GET    /api/notices/:id             # 공지사항 상세
POST   /api/notices                 # 공지사항 작성 (관리자)
PUT    /api/notices/:id             # 공지사항 수정 (관리자)
DELETE /api/notices/:id             # 공지사항 삭제 (관리자)
```

**문의사항 관련** (`/api/attendance-comments`):

```
GET    /api/attendance-comments     # 모든 문의사항 조회
GET    /api/attendance-comments/:id # 문의사항 상세
POST   /api/attendance-comments     # 문의사항 작성 (인증 필요)
PUT    /api/attendance-comments/:id # 문의사항 수정 (작성자/관리자)
DELETE /api/attendance-comments/:id # 문의사항 삭제 (작성자/관리자)
```

---

## 5. 프론트엔드 상세 분석

### 5.1 라우팅 시스템 (`App.jsx`)

#### 라우트 구조

```javascript
/                                    → Home (홈페이지)
/login                               → Login (로그인)
/signup                              → SignUp (회원가입)
/profile                             → Profile (내 정보)

// 학생 페이지
/my-class/status                     → MyClassStatus (수업 현황)
/my-class/:classId/status            → ClassStatusDetail (수업 현황 상세)
/my-class/statistics                  → MyMonthlyStatistics (월별 통계)
/my-class/:classId/monthly-statistics → MyMonthlyStatisticsDetail (월별 통계 상세)

// 학부모 페이지
/parent-class/status                 → ParentClassStatus (수업 현황)
/parent-class/:classId/status        → ParentClassStatusDetail (수업 현황 상세)
/parent-class/statistics             → ParentMonthlyStatistics (월별 통계)
/parent-class/:classId/monthly-statistics → ParentMonthlyStatisticsDetail (월별 통계 상세)

// 관리자 페이지
/admin                               → Admin (관리자 대시보드)
/admin/users                         → UsersList (사용자 관리)
/admin/user/edit/:userId             → UserEdit (사용자 수정)
/admin/classes                       → ClassesList (반 관리)
/admin/class/:classId/records        → ClassRecordManage (수업 기록 관리)
/admin/class-monthly-statistics      → AdminClassMonthlyStatistics (반 월별 통계)
/admin/class/:classId/monthly-statistics → AdminClassMonthlyStatisticsDetail (반 월별 통계 상세)

// 커뮤니티
/community/notice                     → Notice (공지사항)
/community/notice/create             → NoticeCreate (공지사항 작성)
/community/notice/:noticeId          → NoticeDetail (공지사항 상세)
/community/attendance                 → Attendance (문의사항)
```

**특징**:

- 동적 라우팅: `:classId`, `:userId` 파라미터 사용
- 역할별 페이지 분리 (학생, 학부모, 관리자)
- ScrollToTop 컴포넌트로 페이지 전환 시 상단 스크롤

---

### 5.2 API 설정 (`api/axiosConfig.js`)

#### Axios 인스턴스 설정

```javascript
const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || '/api',
  headers: {
    'Content-Type': 'application/json',
  },
});
```

#### 요청 인터셉터

```javascript
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token') || sessionStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

#### 응답 인터셉터

```javascript
api.interceptors.response.use(
  (response) => {
    // 성공 시 마지막 활동 시간 업데이트
    localStorage.setItem('lastActivity', Date.now().toString());
    return response;
  },
  (error) => {
    // 401 에러 시 자동 로그아웃
    if (error.response?.status === 401) {
      localStorage.clear();
      sessionStorage.clear();
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

**원리**:

- 모든 API 요청에 자동으로 토큰 추가
- localStorage/sessionStorage에서 토큰 읽기
- Bearer 토큰 형식으로 헤더 설정
- 401 에러 시 자동 로그아웃 처리

---

### 5.3 공통 컴포넌트 상세

#### Header.jsx

**주요 기능**:

1. **로그인 상태 관리**
   - localStorage에서 사용자 정보 읽기
   - storage 이벤트로 다중 탭 동기화

2. **네비게이션 메뉴**
   - 역할별 메뉴 표시 (학생, 학부모, 관리자)
   - 모바일/데스크톱 반응형 처리

3. **사용자 메뉴**
   - 로그인 시: 환영 메시지, 내 정보, 로그아웃
   - 비로그인 시: 로그인, 회원가입 버튼

**원리**:

- `useState`로 로그인 상태 관리
- `useEffect`로 storage 이벤트 리스너 등록
- 조건부 렌더링으로 역할별 메뉴 표시

---

#### StudentSelector.jsx

**주요 기능**:

- 학부모가 연동된 학생 목록에서 선택
- 학생이 1명이면 자동 선택
- 학생이 여러 명이면 모달 표시
- X 버튼 클릭 시 상단 학생 자동 선택

**구현 상세**:

```javascript
// 학생 목록 조회
const response = await api.get(`/parent-student-links/parent/${parentId}`);

// 학생이 1명이면 자동 선택
if (response.data.data.length === 1 && !selectedStudentId) {
  onSelectStudent(response.data.data[0]);
}

// X 버튼 클릭 시 상단 학생 선택
const handleClose = () => {
  if (students.length > 0) {
    handleSelectStudent(students[0]);
  }
};
```

---

#### ScrollToTop.jsx

**주요 기능**:

- 라우트 변경 시 자동으로 페이지 상단 스크롤

**원리**:

```javascript
useEffect(() => {
  window.scrollTo({ top: 0, behavior: 'instant' });
}, [pathname]);
```

---

### 5.4 페이지 컴포넌트 상세

#### Home.jsx

**구성 섹션**:

1. **Hero Section**
   - 배너 이미지
   - 주요 서비스 소개

2. **ClassSteps Section** (수업 과정)
   - 수업 단계별 설명

3. **Testimonials Section** (수강후기)
   - 캐러셀 형태의 후기 표시
   - PC/모바일 애니메이션 분리

4. **Services Section** (주요 서비스)
   - 서비스 카드

5. **Portfolio Section** (포트폴리오)
   - 성과 및 사례

6. **YouTube Section** (YouTube 영상)
   - YouTube 영상 임베드

**특별한 구현**:

**PC/모바일 애니메이션 분리**:

```javascript
// PC에서만 모든 카드에 애니메이션 적용
useEffect(() => {
  if (isDesktop) {
    // 모든 카드에 Intersection Observer 적용
  }
}, [isDesktop]);

// 모바일에서만 첫 번째 카드에 애니메이션 적용
useEffect(() => {
  if (isMobile) {
    // 첫 번째 카드만 Intersection Observer 적용
  }
}, [isMobile]);
```

---

#### ParentClassStatus.jsx

**주요 기능**:

- 학부모의 연동된 학생 목록 조회
- 학생이 1명이면 자동 선택
- 학생이 여러 명이면 선택 모달 표시
- 선택된 학생의 반 목록 표시
- "변경" 버튼 클릭 시 학생이 1명이면 안내 메시지

**데이터 흐름**:

```javascript
1. 로그인 상태 확인
2. 연동된 학생 목록 가져오기 (/parent-student-links/parent/:parentId)
3. 학생 수에 따라 자동 선택 또는 모달 표시
4. 선택된 학생의 반 정보 가져오기
5. 반 목록 표시
```

---

#### ParentClassStatusDetail.jsx

**주요 기능**:

- 선택된 학생의 수업 현황 상세 조회
- 날짜별 기록 표시
- 반 평균, 최고점, 월별 평균 등 통계 표시
- 선택된 학생 이름 표시

**특별한 구현**:

```javascript
// studentId로 학생 정보 가져와서 이름 표시
const studentResponse = await api.get(`/users/${studentId}`);
setStudentName(studentResponse.data.data.name || '학생');
```

---

#### MyClassStatus.jsx / MyMonthlyStatistics.jsx

**주요 기능**:

- 학생 본인의 수업 현황/월별 통계 조회
- 반 목록 표시
- 반별 상세 조회

---

#### UsersList.jsx

**주요 기능**:

- 모든 사용자 목록 조회 (관리자)
- 사용자 타입별 필터링
- 검색 기능
- 연동 정보 표시 (linkedUsers 배열)
- 페이지네이션

**특징**:

- 연동 정보는 조회만 가능 (수정은 UserEdit에서)
- `linkedUsers` 배열로 모든 연동 정보 표시

---

#### UserEdit.jsx

**주요 기능**:

- 사용자 정보 수정
- 연동 정보 추가/삭제
- 연동 가능한 사용자 검색 및 선택

**특별한 구현**:

```javascript
// 연동 정보 추가
const handleLinkUser = async (targetUserId) => {
  const parentId = formData.userType === '학부모' ? userId : targetUserId;
  const studentId = formData.userType === '학생' ? userId : targetUserId;
  
  await api.post('/parent-student-links', { parentId, studentId });
};

// 연동 해지
const handleUnlinkUser = async (linkId) => {
  await api.delete(`/parent-student-links/${linkId}`);
};
```

---

### 5.5 자동 로그아웃 (`hooks/useAutoLogout.js`)

**주요 기능**:

- 30분 비활성 시 자동 로그아웃
- 활동 감지: 마우스, 키보드, 스크롤, 터치
- 활동 시 타이머 자동 리셋
- 5분 전 경고 알림

**구현 상세**:

```javascript
const INACTIVITY_TIME = 30 * 60 * 1000; // 30분
const WARNING_TIME = 25 * 60 * 1000; // 25분 (5분 전)

useEffect(() => {
  // 활동 감지 이벤트 리스너
  const events = ['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart', 'click'];
  
  const resetTimer = () => {
    localStorage.setItem('lastActivity', Date.now().toString());
    // 타이머 리셋
  };
  
  events.forEach(event => {
    document.addEventListener(event, resetTimer);
  });
  
  // 경고 타이머
  const warningTimer = setTimeout(() => {
    alert('5분 후 자동 로그아웃됩니다.');
  }, WARNING_TIME);
  
  // 로그아웃 타이머
  const logoutTimer = setTimeout(() => {
    localStorage.clear();
    sessionStorage.clear();
    window.location.href = '/';
  }, INACTIVITY_TIME);
  
  return () => {
    events.forEach(event => {
      document.removeEventListener(event, resetTimer);
    });
    clearTimeout(warningTimer);
    clearTimeout(logoutTimer);
  };
}, []);
```

---

## 6. 주요 기능별 상세 설명

### 6.1 사용자 인증 시스템

#### 회원가입 플로우

```
1. 사용자가 회원가입 폼 작성
2. 프론트엔드 유효성 검증
   - 아이디 중복 확인
   - 비밀번호 규칙 확인 (영문+숫자, 7자 이상)
   - 비밀번호 확인 일치
   - 이메일 형식 확인
3. 서버로 회원가입 요청
4. 서버에서 bcrypt로 비밀번호 해싱
5. 학생인 경우:
   - 학부모 계정 자동 생성 (parentContact 기반)
   - ParentStudentLink 자동 생성
6. MongoDB에 저장
7. 성공 응답
```

#### 로그인 플로우

```
1. 사용자가 로그인 폼 작성
2. 서버로 로그인 요청
3. 서버에서 사용자 조회 (userId로)
4. bcrypt로 비밀번호 검증
5. JWT 토큰 발급
   - payload: { userId, id, userType }
   - 만료: 7일
6. 클라이언트에서 localStorage/sessionStorage에 저장
7. 이후 모든 API 요청에 토큰 자동 추가
```

#### 인증 체크

```
1. 클라이언트가 API 요청 시 Authorization 헤더에 토큰 포함
2. 서버의 protect 미들웨어가 토큰 검증
3. 검증 성공 시 req.user에 사용자 정보 저장
4. 다음 미들웨어/컨트롤러에서 req.user 사용
```

---

### 6.2 학생-학부모 n:m 연동 시스템

#### 연동 관계 구조

**데이터베이스 구조**:

```javascript
// ParentStudentLink 컬렉션
[
  { parentId: "부모A_ID", studentId: "학생1_ID" },
  { parentId: "부모B_ID", studentId: "학생1_ID" },
  { parentId: "부모A_ID", studentId: "학생2_ID" }
]
```

**특징**:

- 한 학생이 여러 학부모와 연동 가능 (1:n)
- 한 학부모가 여러 학생과 연동 가능 (n:1)
- 전체적으로는 n:m 관계

#### 연동 생성 플로우

```
1. 관리자 또는 학부모가 연동 요청
2. 서버에서 중복 체크
3. 학부모/학생 유효성 검증
4. ParentStudentLink 생성
5. 유니크 인덱스로 중복 방지
```

#### 학부모 교실 플로우

```
1. 학부모 로그인
2. 연동된 학생 목록 조회 (/parent-student-links/parent/:parentId)
3. 학생이 1명이면:
   - 자동 선택
   - 반 목록 표시
4. 학생이 여러 명이면:
   - 선택 모달 표시
   - 학생 선택 후 반 목록 표시
5. 반 클릭 시 상세 페이지로 이동 (studentId 파라미터 포함)
```

---

### 6.3 수업 현황 및 통계 시스템

#### 수업 현황 조회 플로우

```
1. 학생/학부모가 수업 현황 페이지 접속
2. 반 목록 조회
3. 반 클릭 시 상세 페이지로 이동
4. 날짜별 기록 조회:
   - 리뷰TEST 점수
   - 실전TEST 점수
   - 과제
   - 특이사항
5. 통계 계산:
   - 반 평균
   - 최고점
   - 월별 평균
```

#### 월별 통계 조회 플로우

```
1. 학생/학부모가 월별 통계 페이지 접속
2. 반 목록 조회
3. 반 클릭 시 상세 페이지로 이동
4. 연도/월 선택
5. 해당 월의 모든 날짜별 기록 조회
6. 통계 계산 및 표시
```

---

### 6.4 관리자 시스템

#### 사용자 관리

- 사용자 목록 조회 (페이지네이션, 검색, 필터링)
- 사용자 추가/수정/삭제
- 연동 정보 조회/추가/삭제

#### 반 관리

- 반 목록 조회
- 반 추가/수정/삭제
- 반에 학생 추가/제거

#### 수업 기록 관리

- 학생별 기록 추가/수정/삭제
- 반별 수업 내용 기록
- 날짜별 기록 관리

---

## 7. 데이터 흐름 및 원리

### 7.1 전체 데이터 흐름도

```
[사용자]
    ↓
[프론트엔드 (React)]
    ↓ (HTTP Request)
[Axios Interceptor] → 토큰 자동 추가
    ↓
[백엔드 (Express)]
    ↓
[인증 미들웨어] → JWT 검증
    ↓
[권한 미들웨어] → 역할 체크 (필요시)
    ↓
[컨트롤러] → 비즈니스 로직
    ↓
[Mongoose Model] → 데이터 검증
    ↓
[MongoDB] → 데이터 저장/조회
    ↓
[응답] → JSON 형식
    ↓
[프론트엔드] → UI 업데이트
```

---

### 7.2 인증 흐름 상세

```
┌─────────────────────────────────────────┐
│ 1. 로그인 요청                           │
│    POST /api/users/login                │
│    { userId, password }                 │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 2. 서버 검증                             │
│    - User.findOne({ userId })           │
│    - bcrypt.compare(password, hash)     │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 3. JWT 토큰 발급                         │
│    jwt.sign({                            │
│      userId, id, userType               │
│    }, SECRET, { expiresIn: '7d' })      │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 4. 클라이언트 저장                       │
│    localStorage.setItem('token', ...)    │
│    localStorage.setItem('user', ...)    │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 5. 이후 API 요청                         │
│    Authorization: Bearer {token}         │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 6. 서버 검증                             │
│    jwt.verify(token, SECRET)            │
│    User.findById(decoded.id)            │
│    req.user = user                      │
└─────────────────────────────────────────┘
```

---

### 7.3 학부모 학생 선택 흐름

```
┌─────────────────────────────────────────┐
│ 학부모가 수업 현황 페이지 접속          │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ GET /api/parent-student-links/parent/:id│
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ ParentStudentLink.find({ parentId })     │
│ .populate('studentId')                  │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 학생 목록 반환                           │
│ [{ student1 }, { student2 }, ...]      │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 학생 수 확인                             │
│ - 1명: 자동 선택                         │
│ - 여러 명: 모달 표시                     │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 선택된 학생의 반 정보 조회               │
│ GET /api/users/:studentId                │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 반 목록 표시                             │
│ 반 클릭 시 상세 페이지로 이동            │
│ (studentId 파라미터 포함)                │
└─────────────────────────────────────────┘
```

---

### 7.4 수업 기록 조회 흐름

```
┌─────────────────────────────────────────┐
│ 학생/학부모가 수업 현황 상세 페이지 접속 │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ GET /api/student-records/my-records     │
│ ?classId=xxx&date=yyyy-mm-dd            │
│ &studentId=xxx (학부모인 경우)          │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 권한 확인                                │
│ - 학생: 본인 기록만 조회 가능            │
│ - 학부모: 연동된 학생 기록만 조회 가능   │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ StudentRecord.find({                     │
│   studentId, classId, date               │
│ })                                       │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 통계 계산                                │
│ - 반 평균                                │
│ - 최고점                                 │
│ - 월별 평균                              │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ UI 렌더링                                │
│ - 날짜별 기록 표시                       │
│ - 통계 표시                              │
└─────────────────────────────────────────┘
```

---

## 8. 보안 및 성능 최적화

### 8.1 보안 구현

#### 비밀번호 보안

- **bcrypt 해싱**: salt rounds 10
- **최소 길이**: 7자
- **복잡도 요구**: 영문 + 숫자

#### 인증 보안

- **JWT 토큰**: 7일 만료
- **Bearer 토큰**: Authorization 헤더 사용
- **토큰 검증**: 모든 보호된 라우트에서 검증
- **자동 로그아웃**: 30분 비활성 시

#### 권한 관리

- **역할 기반 접근 제어 (RBAC)**:
  - 학생: 본인 데이터만 접근
  - 학부모: 연동된 자녀 데이터만 접근
  - 강사(관리자): 모든 데이터 접근 가능
- **이중 체크**: `protect` + `authorize`

#### 입력 검증

- **Mongoose 스키마 검증**: 데이터 타입, 필수 필드
- **이메일 형식 검증**: 정규식으로 형식 확인
- **전화번호 형식 검증**: 숫자와 하이픈만 허용
- **에러 처리**: 사용자 친화적 에러 메시지

#### 개인정보 보호

- **개인정보 처리 로그**: PrivacyLog 모델로 모든 처리 기록
- **비밀번호 제외**: API 응답에서 비밀번호 필드 제외
- **연동 정보 보호**: 학부모는 연동된 학생만 조회 가능

---

### 8.2 성능 최적화

#### 프론트엔드 최적화

**1. 상태 관리 최적화**

- useCallback으로 함수 메모이제이션
- 조건부 렌더링으로 불필요한 렌더링 방지

**2. 네트워크 최적화**

- 페이지네이션으로 데이터 분할 로딩
- 필요한 데이터만 가져오기 (limit 파라미터)

**3. 렌더링 최적화**

- 조건부 렌더링
- 로딩 상태 표시
- Intersection Observer로 애니메이션 최적화

**4. 자동 로그아웃 최적화**

- 이벤트 리스너 디바운싱
- 타이머 최적화

#### 백엔드 최적화

**1. 데이터베이스 최적화**

- 인덱스 사용:
  - `userId` (unique)
  - `parentId`, `studentId` (연동 관계 조회)
  - `{ parentId, studentId }` (복합 유니크)
  - `createdAt` (정렬 최적화)
- MongoDB aggregation으로 효율적 쿼리
- populate로 관계 데이터 효율적 조회

**2. API 최적화**

- 페이지네이션으로 대량 데이터 처리
- lean()으로 Mongoose 문서 대신 일반 객체 반환 (성능 향상)

**3. 캐싱**

- localStorage/sessionStorage로 사용자 정보 캐싱
- 마지막 활동 시간 저장

---

## 9. 특별한 구현 사항

### 9.1 학생-학부모 n:m 연동 관계

**문제**: 형제자매가 함께 수강하거나, 한 학생이 여러 학부모(부모님, 조부모님 등)와 연동되어야 하는 경우

**해결책**:

```javascript
// ParentStudentLink 모델로 n:m 관계 구현
const parentStudentLinkSchema = new mongoose.Schema({
  parentId: { type: ObjectId, ref: 'User', required: true, index: true },
  studentId: { type: ObjectId, ref: 'User', required: true, index: true },
});

// 복합 유니크 인덱스로 중복 방지
parentStudentLinkSchema.index({ parentId: 1, studentId: 1 }, { unique: true });
```

**특징**:

- 한 학생이 여러 학부모와 연동 가능
- 한 학부모가 여러 학생과 연동 가능
- 각 연동 관계가 독립적으로 관리됨
- 연동 해지 시 다른 연동 관계는 영향 없음

---

### 9.2 학부모 학생 선택 모달

**주요 기능**:

1. **자동 선택**: 학생이 1명이면 자동 선택
2. **모달 표시**: 학생이 여러 명이면 선택 모달 표시
3. **X 버튼**: X 버튼 클릭 시 상단 학생 자동 선택
4. **변경 버튼**: 학생이 1명이면 변경 버튼 클릭 시 안내 메시지

**구현 상세**:

```javascript
// 학생 목록 조회
const studentsResponse = await api.get(`/parent-student-links/parent/${userData._id}`);

if (students.length === 1) {
  // 자동 선택
  setSelectedStudent(students[0]);
} else {
  // 모달 표시
  setShowStudentSelector(true);
}

// X 버튼 클릭 시
const handleClose = () => {
  if (students.length > 0) {
    handleSelectStudent(students[0]); // 상단 학생 선택
  }
};

// 변경 버튼 클릭 시
const handleChangeStudent = () => {
  if (linkedStudents.length === 1) {
    alert('연동된 학생회원이 한 명뿐입니다.');
    return;
  }
  setShowStudentSelector(true);
};
```

---

### 9.3 자동 로그아웃 시스템

**주요 기능**:

- 30분 비활성 시 자동 로그아웃
- 활동 감지: 마우스, 키보드, 스크롤, 터치, 클릭
- 활동 시 타이머 자동 리셋
- 5분 전 경고 알림

**구현 상세**:

```javascript
const INACTIVITY_TIME = 30 * 60 * 1000; // 30분
const WARNING_TIME = 25 * 60 * 1000; // 25분 (5분 전)

useEffect(() => {
  const events = ['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart', 'click'];
  
  const resetTimer = () => {
    localStorage.setItem('lastActivity', Date.now().toString());
    // 타이머 리셋
  };
  
  events.forEach(event => {
    document.addEventListener(event, resetTimer);
  });
  
  // 경고 타이머
  const warningTimer = setTimeout(() => {
    alert('5분 후 자동 로그아웃됩니다.');
  }, WARNING_TIME);
  
  // 로그아웃 타이머
  const logoutTimer = setTimeout(() => {
    localStorage.clear();
    sessionStorage.clear();
    window.location.href = '/';
  }, INACTIVITY_TIME);
  
  return () => {
    events.forEach(event => {
      document.removeEventListener(event, resetTimer);
    });
    clearTimeout(warningTimer);
    clearTimeout(logoutTimer);
  };
}, []);
```

**특징**:

- Axios 인터셉터와 연동하여 API 요청 시에도 활동 시간 업데이트
- 401 에러 시 자동 로그아웃 처리

---

### 9.4 PC/모바일 애니메이션 분리

**문제**: PC와 모바일에서 다른 애니메이션 효과 필요

**해결책**:

```javascript
// 화면 크기 감지
const [isDesktop, setIsDesktop] = useState(window.innerWidth >= 641);
const [isMobile, setIsMobile] = useState(window.innerWidth <= 640);

useEffect(() => {
  const handleResize = () => {
    setIsDesktop(window.innerWidth >= 641);
    setIsMobile(window.innerWidth <= 640);
  };
  
  window.addEventListener('resize', handleResize);
  return () => window.removeEventListener('resize', handleResize);
}, []);

// PC에서만 모든 카드에 애니메이션 적용
useEffect(() => {
  if (isDesktop) {
    // 모든 카드에 Intersection Observer 적용
    cardsRef.current.forEach((card) => {
      observer.observe(card);
    });
  }
}, [isDesktop]);

// 모바일에서만 첫 번째 카드에 애니메이션 적용
useEffect(() => {
  if (isMobile && cardsRef.current[0]) {
    observer.observe(cardsRef.current[0]);
  }
}, [isMobile]);
```

---

### 9.5 학생 회원가입 시 학부모 계정 자동 생성

**주요 기능**:

- 학생 회원가입 시 학부모 계정 자동 생성
- ParentStudentLink 자동 생성
- 기존 학부모 계정이 있으면 재사용

**구현 상세**:

```javascript
// 학생인 경우 연동된 학부모 계정 자동 생성
if (userType === '학생') {
  const parentUserId = parentContact.trim();
  const parentEmail = `${parentUserId}@mathchang.com`;
  
  const existingParent = await User.findOne({
    $or: [
      { userId: parentUserId },
      { email: parentEmail }
    ]
  });
  
  if (!existingParent) {
    // 학부모 계정 생성
    const parentPassword = parentContact.trim();
    const hashedParentPassword = await bcrypt.hash(parentPassword, saltRounds);
    
    const parentUser = new User({
      userId: parentUserId,
      password: hashedParentPassword,
      name: `${name}부모님`,
      email: parentEmail,
      phone: parentContact,
      userType: '학부모',
      // ...
    });
    
    await parentUser.save();
  }
  
  // ParentStudentLink 생성
  const link = new ParentStudentLink({
    parentId: parentUser._id,
    studentId: savedUser._id,
  });
  
  await link.save();
}
```

---

### 9.6 학생 삭제 시 연동 정보 정리

**주요 기능**:

- 학생 삭제 시 연동된 학부모 계정은 유지
- 연동 정보만 제거
- 학부모의 studentContact 초기화

**구현 상세**:

```javascript
// 연동된 학부모 찾기
const parentLinks = await ParentStudentLink.find({ studentId: user._id });

// 각 학부모의 studentContact 초기화
for (const link of parentLinks) {
  await User.findByIdAndUpdate(link.parentId, {
    studentContact: '000-0000-0000'
  });
}

// ParentStudentLink 삭제
await ParentStudentLink.deleteMany({ studentId: user._id });
```

---

## 10. 배포 및 환경 설정

### 10.1 환경 변수

**백엔드 (`.env`)**:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/mathchang
MONGODB_ATLAS_URL=mongodb+srv://username:password@cluster.mongodb.net/mathchang
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d
NODE_ENV=production
```

**프론트엔드 (`.env`)**:

```env
VITE_API_URL=https://your-backend-url.herokuapp.com/api
```

---

### 10.2 배포 과정

**백엔드 (Heroku)**:

1. Heroku 앱 생성
2. 환경 변수 설정 (Heroku Dashboard)
3. Git push로 배포

**프론트엔드 (Vercel)**:

1. GitHub 저장소 연결
2. `client` 폴더를 루트로 설정
3. 환경 변수 설정
4. 자동 배포

---

## 11. 향후 개선 사항

### 11.1 기능 개선

- [ ] 실시간 알림 시스템
- [ ] 파일 업로드 기능 (과제 제출 등)
- [ ] 차트/그래프로 통계 시각화
- [ ] 엑셀 내보내기 기능

### 11.2 성능 개선

- [ ] 이미지 최적화 (WebP 형식)
- [ ] 코드 스플리팅
- [ ] 서버 사이드 렌더링 (SSR)
- [ ] 캐싱 전략 개선 (Redis)

### 11.3 UX 개선

- [ ] 다크 모드
- [ ] 접근성 개선 (ARIA)
- [ ] PWA 지원
- [ ] 오프라인 지원

---

## 12. 결론

MathChang 프로젝트는 **현대적인 웹 기술 스택**을 활용하여 구현된 **완전한 풀스택 웹 애플리케이션**입니다.

**주요 강점**:

- ✅ 깔끔하고 모듈화된 코드 구조
- ✅ 강력한 인증 및 권한 관리
- ✅ 유연한 n:m 연동 관계 시스템
- ✅ 역할 기반 접근 제어
- ✅ 반응형 디자인
- ✅ 자동 로그아웃 보안 기능
- ✅ 완벽한 에러 처리

**기술적 특징**:

- React + Node.js 기반 풀스택 개발
- JWT 기반 인증 시스템
- MongoDB를 활용한 유연한 데이터 구조
- n:m 관계를 지원하는 연동 시스템
- 반응형 및 모바일 최적화

이 프로젝트는 **실제 비즈니스 요구사항**을 충족하면서도 **최신 웹 개발 트렌드**를 반영한 우수한 사례입니다.

---

**문서 작성일**: 2025년  
**작성자**: AI Assistant  
**프로젝트 버전**: 1.0.0

